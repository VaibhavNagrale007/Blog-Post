<%- include("partials/header") -%> 

    <div class="content">
        <h1>Home</h1>
        <p><%= homeText %></p>
        <% for (var i=0; i<journals.length; i++){ %>
            <div>
                <h1><%= journals[i].tle %></h1>
                <p><%= journals[i].cont.substring(0, 100)+" ..." %>
                <a href="/posts/<%=journals[i].tle%>">Read More</a></p>
            </div>
    <% } %>
    </div>

<%- include("partials/footer") -%>